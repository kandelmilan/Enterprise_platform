name: enterprise_platform
description: Enterprise Platform Flutter Application
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.11.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  get: ^4.7.2
  dio: ^5.10.0
  flutter_secure_storage: ^9.2.2
  requests_inspector: ^5.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
flutter:
  uses-material-design: true

  assets:
    - .env